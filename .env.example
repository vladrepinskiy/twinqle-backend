# Server
PORT=5001
NODE_ENV=development

# Database (matches docker-compose.yml)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=twinqle
DB_USER=twinqle
DB_PASSWORD=twinqle_password

# Late Logistics Integration - even though it's a mock API not feeling like putting it in a public repo
LATE_LOGISTICS_API_URL=https://valid-carrier-api-url.com
LATE_LOGISTICS_API_KEY=your_valid_api_key 

# Webhook URL - Get this from ngrok
# After running `ngrok http 5001`, copy the HTTPS URL shown in the output
WEBHOOK_BASE_URL=https://abc123def456.ngrok-free.app
